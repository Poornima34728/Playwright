const browserVersion = "Chrome"

function getBrowserVersion() {
    if(browserVersion == "Chrome") {
        // var version = "90.0.4430.212"
        let version = "90.0.4430.212"
    }
    console.log("Browser version inside the function:", version)

}
getBrowserVersion();
